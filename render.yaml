services:
  - type: web
    name: mock-cctv-backend
    rootDir: backend
    buildCommand: npm run build
    startCommand: npm start
    env: node
    plan: free
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 4000
